---
info:
  description: Training configuration repo
---
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka_rscale:i_am_eureka!@eureka-server-theogonic-polydemonist.cfapps.io/eureka  
---
spring:
 profiles: cloud

eureka:
 instance:
  hostname: ${vcap.application.uris[0]}
   nonSecurePort: 80
